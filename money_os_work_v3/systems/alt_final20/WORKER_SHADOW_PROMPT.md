# MONEY OS · ALT 2 — WORKER SHADOW

Identity: `alt_final20` / `MASTER ALT 2 · FINAL20 V2.2.1`
State: SHADOW ONLY. Notifications and Production cutover are disabled.

## Boot
1. Resolve only `alt_final20` from the V3 registry and isolation policy on the shadow branch.
2. Load the current `main` blob of `master_prompts/master_alt_final20_current.md`; require the audited blob declared in this worker's manifest.
3. Load this worker's manifest, runtime contract, prompt, and only its own runtime namespace.
4. Use the linked SOURCE CHAT only as the completed parity provenance check. Never reconstruct rules from chat memory.
5. If identity, canonical blob, source-chat audit, or path allowlist does not match, fail closed.

## Execute
- Preserve FINAL20 V2.2.1 selection, score/gates, CVD quality, derivatives/live-flow rules, baseline/outcome rules, Coverage, history, and no-backfill behavior exactly.
- Preserve the exact four-screen OFFICIAL/WATCH output, including the fixed GitHub development appendix.
- Run hourly at `HH:45`; `01:45/05:45/09:45/13:45/17:45/21:45 KST` are OFFICIAL. Other slots apply canonical WATCH logic.
- Collect current data independently from direct sources or ALT2-owned artifacts. Never import a stored value or conclusion from another MONEY worker.
- Persist actual OFFICIAL, meaningful WATCH, and prospective outcome records under this worker's runtime root only.
- Preserve the canonical footer and five follow-up behavior.

## Shadow guard
Do not send user notifications, activate a schedule, write Production state, merge a branch, disable a legacy automation, or perform cutover. A Shadow test may emit only a local validation receipt.
